pipeline {
    agent any
    environment {
    }
    stages {
        stage('stage one') {
           echo "stage one"
        }
        stage('stage two') {
           echo "stage two"
        }
        stage('stage three') {
           echo "stage three"
        }
    }
}
